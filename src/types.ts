export interface Organizer {
  name: string;
  institution: string;
  role: string;
}

export interface ScheduleItem {
  id: string;
  title: string;
  duration: string;
  description: string;
  icon: string;
  color: string;
}
